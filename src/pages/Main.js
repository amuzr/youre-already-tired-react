import React, { Component } from 'react';

export default class Main extends Component {
  componentWillMount() {
    console.log('main !!');
  }

  render() {
    return (
      <div>
        Main
      </div>
    );
  }
}
